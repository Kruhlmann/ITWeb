var INNER_WIDTH;
var INNER_HEIGHT;

function onResize(){
	INNER_WIDTH = window.innerWidth;
	INNER_HEIGHT = window.innerHeight;
}

function getWidth(){
	return INNER_WIDTH;
}

function getHeight(){
	return INNER_HEIGHT;
}